# Mapbox access token for the Employsi map (src/employsi).
# Get one at https://account.mapbox.com/access-tokens/
# Copy this file to `.env` (or `.env.local`) and fill in your token.
VITE_MAPBOX_TOKEN=
